# Cardgame Engine

Generic cardgame engine in Python.

## Status

Early scaffold (v0.0.0). Project structure, tooling and tests are set up.

## Dev setup

Create a virtual environment and install the project (editable) with dev tools:

```bash
python -m venv .venv

# Windows (PowerShell / CMD):
.venv\Scripts\activate

# macOS/Linux:
# source .venv/bin/activate

pip install -e ".[dev]"
